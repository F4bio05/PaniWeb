import { View } from 'react-native'
import React from 'react'

const Separator = ({height}) => {
  return (
    <View style={[{ width: '100%', height: height*1}, {backgroundColor: ''}]} >
      
    </View>
  )
}
export default Separator